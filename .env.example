# =============================================================================
# A股波段交易筛选系统 - 环境变量配置
# =============================================================================
# 使用说明：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 填入你的实际配置值
# 3. 确保 .env 已被添加到 .gitignore（非常重要！）
# =============================================================================

# -----------------------------------------------------------------------------
# AI 服务配置 (智谱AI GLM-4-Flash)
# -----------------------------------------------------------------------------
# 必填: 从 https://open.bigmodel.cn/ 获取你的 API Key
# 注意: 不要把真实 key 提交到 git！
GLM_API_KEY=your_glm_api_key_here
# 备选变量名（向后兼容）
ZHIPUAI_API_KEY=your_zhipuai_api_key_here

# 可选: 模型配置
GLM_MODEL=glm-4-flash
GLM_MODEL_TEMPERATURE=0.7
GLM_MODEL_MAX_TOKENS=4096

# -----------------------------------------------------------------------------
# CORS 跨域配置
# -----------------------------------------------------------------------------
# 可选: 允许的前端地址，多个地址用逗号分隔
# 默认值: http://localhost:5173,http://localhost:3000
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# -----------------------------------------------------------------------------
# 服务器配置
# -----------------------------------------------------------------------------
# 可选: 服务器端口（默认 8000）
PORT=8000

# 可选: 服务器主机（默认 0.0.0.0）
HOST=0.0.0.0

# -----------------------------------------------------------------------------
# 数据缓存配置
# -----------------------------------------------------------------------------
# 可选: 股票数据缓存时间（秒，默认 60）
CACHE_TTL_STOCK=60

# 可选: 市场环境数据缓存时间（秒，默认 300）
CACHE_TTL_MARKET=300

# -----------------------------------------------------------------------------
# 日志配置
# -----------------------------------------------------------------------------
# 可选: 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 可选: 日志文件路径
LOG_FILE=logs/app.log
